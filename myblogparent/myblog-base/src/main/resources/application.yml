server:
  port: 9001

  #服务名，只能是横岗，不能是下划线
spring:
  application:
    name: myblog-base

datasource:
  driverClassName: com.mysql.jdbc.Driver
  url: jdbc:mysql://192.168.92.133:3306/myblog_base?
  characterEncoding=utf‐8
  username: root
  password: root

jpa:
database: MySQL
show‐sql: true
