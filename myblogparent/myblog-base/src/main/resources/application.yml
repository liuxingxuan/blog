server:
  port: 9001


spring:
  application:
    name: myblog-base #服务名，只能是横岗，不能是下划线

datasource:
  driverClassName: com.mysql.jdbc.Driver
  url: jdbc:mysql://192.168.92.133:3306//tensquare_base?
  characterEncoding=utf-8
  username: root
  password: root

jpa:
database: MySQL
show‐sql: true
